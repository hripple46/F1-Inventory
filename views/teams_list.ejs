<!DOCTYPE html>
<html class="h-full w-full">
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body class="w-full h-full">
    <div class="flex w-full h-full">
      <%- include ('sidebar') %>
      <div class="w-3/4 pt-8 pl-4">
        <h1 class="text-red-600 h-24 text-3xl"><%= title %></h1>
        <p class="text-2xl"><%= title %> Teams:</p>
        <div class="grid grid-cols-3 gap-4 mt-8">
          <% for(let i = 0; i < teams_list.length; i++) { %>
          <a href="<%= teams_list[i].url %>">
            <div
              class="flex items-center min-h-[90px] h-full w-full bg-red-600 text-white rounded-lg"
            >
              <p class="text-xl"><%= teams_list[i].name %></p>
            </div></a
          >
          <% } %>
        </div>
      </div>
    </div>
  </body>
</html>
